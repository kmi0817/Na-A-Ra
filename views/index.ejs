<!DOCTYPE html>
<html lang="ko">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="IE=Edge">
    <title>admin</title>

    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css" integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm" crossorigin="anonymous">

    <style>
        a {
            text-decoration: none;
            color: rgb(63, 63, 63);
        }
    </style>
</head>

<body>
    <div class="container">
        <h2 class="mb-4"><a href="/">나아라</a></h2>

        <a href="/" class="btn btn-dark">로그인</a>
        <a href="/" class="btn btn-outline-dark">회원가입</a>
        <a href="/openapi" class="btn btn-light">건강보험심사평가원 OpenAPI</a>

        <div class="card mt-4">
            <div class="card-body">
                <form action="" method="GET">
                    <!-- <div class="form-group">
                        <select name="search_type">
                            <option value="name">병원명</option>
                        </select>
                    </div> -->
                    <div class="form-group">
                        <button type="submit" class="d-inline btn btn-info">검색</button>
                    </div>
                    <div class="form-group">
                        <label for="search_input">병원이름</label>
                        <input required value="<%= results.title %>" type="text" name="seacrh_input" id="seacrh_input" class="form-control">
                    </div>
                </form>
            </div>
        </div>

        <% if (results) { %>
            <div class="card mt-4">
                <div class="card-body">
                    <% if (Array.isArray(results)) { %>
                        <% results.forEach((result) => { %>
                            <div class="row">
                                <h4 class="col-md-8 card-title"><%= result.name %></h4>
                                <a href="/admin/hospitals/<%= result._id %>" class="col-md-4 btn btn-outline-primary">자세히 보기</a>
                            </div>
                        
                            <div class="card-subtitle text-muted mb-2">
                                <%= result.type_code %>
                            </div>
                            <div class="row">
                                <div class="col-md-8 card-text mb-2">
                                    주소: <%= result.addr %>
                                </div>
                                <div class="col-md-4 card-text mb-2">
                                    번호: <%= result.telno %>
                                </div>
                            </div>
                        <% }); %>
                    <% } else { %>
                        <div class="card-text mb-2">
                            <h4>일치하는 검색 결과가 없습니다.</h4>
                        </div>
                    <% } %>
                </div>
            </div>                
        <% } %>
    </div>
</body>

</html>